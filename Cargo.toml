[package]
name = "bort"
version = "0.1.0"
edition = "2021"

[dependencies]
# ash is a lightweight vulkan wrapper
ash = "0.37.2"
ash-window = "0.12"
raw-window-handle = "0.5"
# AMD vulkan memory allocator bindings
vk-mem = { version = "0.2.3", path = "../vk-mem-bort" }